language: ruby
rvm:
  - 1.8.7
  - 1.9.3
  - 2.0.0
  - ruby-head
matrix:
  allow_failures:
    - rvm: ruby-head
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq ruby1.8-dev ruby1.9-dev ruby1.9.1-dev ruby2.0-dev

